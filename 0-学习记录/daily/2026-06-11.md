# 2026-06-11

## 做了什么

- 学习 ROS2 机器人开发基础流程。
- 创建并编译 `~/ros2_ws` 工作空间。
- 创建 Python 功能包 `my_package`，实现 `learning_talker`，验证 `/learning_chatter`。
- 创建 C++ 功能包 `my_cpp_pkg`，实现 `my_node`，验证 `/cpp_chatter`。
- 对 `my_cpp_pkg` 执行 `colcon test`，结果通过。
- 阅读并执行 TurtleBot3 初步操控 PDF。
- 安装 Gazebo Harmonic、ROS-GZ、TurtleBot3、Cartographer、Navigation2。
- 配置 `TURTLEBOT3_MODEL=waffle` 和 `ROS_DOMAIN_ID=30`。
- 跑通 `ros_gz_sim_demos diff_drive.launch.py`。
- 跑通 `ros_gz_sim_demos rgbd_camera_bridge.launch.py`。
- 启动 `turtlebot3_gazebo turtlebot3_world.launch.py`。
- 验证 `/scan`、`/camera/camera_info`、`/odom`、`/cmd_vel`。
- 通过 `/cmd_vel` 控制 TurtleBot3 移动，并用 `/odom` 确认位姿变化。
- 启动 Cartographer SLAM，生成 `/map`。
- 保存地图到 `/home/ubuntu/tb3_slam_test.yaml` 和 `/home/ubuntu/tb3_slam_test.pgm`。
- 使用保存的地图启动 Navigation2。
- 发布初始位姿后验证 `/amcl_pose` 和 `map -> base_link` 变换。
- 从 0 创建 `differential_drive_robot` 两轮差速机器人项目。
- 实现 URDF 模型、Gazebo world、ROS-GZ bridge、launch、RViz 配置。
- 实现 C++ 节点 `robot_controller` 和 `teleop_keyboard`。
- 验证 `/teleop_cmd -> /cmd_vel -> Gazebo -> /odom` 闭环。
- 修复 URDF 根 link 惯性 warning，改成 `base_footprint -> base_link`。
- `colcon test --packages-select differential_drive_robot` 通过。
- 将本地学习记录和 `differential_drive_robot` 推送到 GitHub 仓库 `Edddddddddy/rosyay`。
- 阅读 `4-服务与参数管理.pdf`。
- 为 `differential_drive_robot` 增加自定义服务 `SetControlMode.srv`。
- 增加 `/set_control_mode` 服务。
- 增加 `/control_mode` 模式切换话题。
- 增加参数配置 `controller_params.yaml`。
- 实现 `manual`、`auto_forward`、`auto_circle`、`obstacle_avoidance`、`wall_following` 控制模式。
- 验证服务调用切换 `auto_circle` 后 `/cmd_vel` 和 `/odom` 正常变化。

## 当前结果

- WSL2 Ubuntu 24.04 + ROS2 Jazzy 基础环境可用。
- Gazebo Sim 版本为 `8.13.0`。
- TurtleBot3 仿真、传感器、速度控制、SLAM、地图保存、Navigation2 启动均已跑通。
- Navigation2 日志出现 `Managed nodes are active`。
- `differential_drive_robot` 已能编译、启动和控制移动。
- `ros2 pkg executables differential_drive_robot` 能发现：
  - `robot_controller`
  - `teleop_keyboard`
- 发送 `/teleop_cmd` 后 `/odom` 从接近 0 变化到 `x≈0.1503, y≈0.0259`。
- `differential_drive_robot` 测试结果：6 tests，0 errors，0 failures。
- GitHub 仓库已初始化并推送：`https://github.com/Edddddddddy/rosyay`
- 服务与参数管理升级后，`differential_drive_robot` 仍然构建和测试通过。
- 保存的测试地图位置：

```text
/home/ubuntu/tb3_slam_test.yaml
/home/ubuntu/tb3_slam_test.pgm
```

## 踩坑

- 从 PowerShell 调 WSL 时，`$HOME`、`$t`、`$!`、`$(...)` 容易被 PowerShell 抢先展开。
- 教程里的 Gazebo 旧包名要按 Jazzy/Harmonic 改成 `gz-harmonic` 和 `ros-jazzy-ros-gz`。
- `ros_gz_sim_demos diff_drive` 的速度话题是 `Twist`，TurtleBot3 的 `/cmd_vel` 是 `TwistStamped`。
- Navigation2 启动后没有初始位姿时，`map -> base_link` 不存在是正常现象，需要用 RViz 的 `2D Pose Estimate` 或命令行发布 `/initialpose`。
- 复杂 ROS2 YAML 消息跨 PowerShell/WSL 容易被引号破坏，必要时用 `rclpy` 小脚本发布。
- Gazebo Harmonic 插件名要使用 `gz-sim-*` 当前写法。
- URDF 根 link 不建议带惯性，增加 `base_footprint` 更稳。
- ament `uncrustify` 对短 lambda 格式严格，需要按工具输出修。
- 自定义 service 需要配置 `rosidl_default_generators`、`rosidl_default_runtime` 和 typesupport 链接。
- `package.xml` 中 `member_of_group` 需要放在 `<export>` 前面。

## 下一步

1. 手动练习 `ros2 run turtlebot3_teleop teleop_keyboard`。
2. 在 RViz 里练习 `2D Pose Estimate` 和 `2D Goal Pose`。
3. 观察 `/tf`、`/amcl_pose`、`/plan`、`/cmd_vel_smoothed`。
4. 做一次完整的导航目标点实验。
5. 手动打开 `differential_drive_robot` 的 Gazebo + RViz。
6. 用 `teleop_keyboard` 手动控制自制差速机器人。
7. 给 `robot_controller` 增加参数化安全策略。

## 导航与运动指令推进

- 阅读 `5-导航与运动指令.pdf`，确认本章重点是 Navigation2 架构、坐标系、参数文件、Gazebo/SLAM/Nav2 启动，以及 C++ `NavigateToPose` 客户端。
- 创建 `nav2_cpp_tutorial` C++ 功能包。
- 实现 `Nav2Client`，封装 `nav2_msgs/action/NavigateToPose`。
- 实现 `nav2_simple_test`，用于检查 Nav2 action server 并发送示例目标。
- 编写 `nav2_params.yaml` 和 `nav2_cpp_demo.launch.py`。
- 修复 Jazzy 插件类名、DWB critics、`collision_monitor`、`docking_server` 等参数问题。
- 验证 `colcon build --symlink-install --packages-select nav2_cpp_tutorial` 通过。
- 验证 `colcon test --packages-select nav2_cpp_tutorial` 通过。
- 验证启动链路中 `lifecycle_manager_slam` 和 `lifecycle_manager_navigation` 都进入 `Managed nodes are active`。
- 建立本轮功能分支：`feature/navigation-motion-commands`。

## 学习文档整理与课程源码对比

- 按 GitHub 流程创建分支：`feature/docs-course-comparison`。
- 下载课程源码仓库到 `C:\Users\lcy\code\ros2\course-src`。
- 对比课程 `differential_drive_robot` 与当前实现：当前项目更适配 Jazzy/Harmonic，并已增加 service、参数和 RViz 配置。
- 对比课程 `turtle_bot3` 与当前实现：课程更强调自研感知、A* 路径规划、路径跟随；当前项目更强调官方 Nav2 bringup 与 action client。
- 新增 `08-阶段总结与课程源码对比.md`。
- 重写学习记录 `README.md`，整理主线笔记、辅助记录、源码位置、外部课程源码和 Git 分支规则。

下一步建议：

1. 完成 RViz 真实目标点导航验证。
2. 给 `nav2_simple_test` 增加可配置目标点。
3. 新建自研 `turtlebot3_navigation_tutorial` 学习包，补传感器处理、A*、路径跟随。
4. 后续继续做自定义 action 和 DDS/QoS 专题。
