# 2026-06-10

## 做了什么

- 查看 `ros2-doc` 目录结构，确认已有 ROS2 课程资料按章节存放。
- 安装 Codex skills：`pdf`、`jupyter-notebook`、`screenshot`。
- 创建学习记录目录：`0-学习记录`。
- 安装 Ubuntu 24.04 LTS Windows 应用包。
- 测试 WSL2 发行版注册与启动。
- 安装 ROS 2 Jazzy Desktop。
- 安装 ROS 开发工具 `ros-dev-tools`。
- 初始化 `rosdep`。
- 跑通 talker/listener、turtlesim、rviz2。

## 当前结果

- Ubuntu 24.04 应用包安装成功，并已注册为 WSL2 发行版。
- WSL 默认用户已切换为 `ubuntu`。
- WSLg GUI 通道可用：`DISPLAY=:0`，`WAYLAND_DISPLAY=wayland-0`。
- 已安装 `x11-apps`、`mesa-utils`。
- `glxinfo -B` 能看到 `D3D12 (NVIDIA GeForce RTX 3060 Ti)`。
- ROS 2 Jazzy Desktop 安装成功。
- `ROS_DISTRO=jazzy`。
- `colcon`、`rosdep` 可用。
- `rviz2` 可启动，OpenGL 4.5。

## 下一步

1. 手动练习 turtlesim 键盘控制。
2. 整理 ROS2 基础命令：node、topic、service、param。
3. 进入 TurtleBot3/Gazebo 仿真配置。
