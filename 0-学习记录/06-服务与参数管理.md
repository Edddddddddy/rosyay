# 06 服务与参数管理

日期：2026-06-11

参考教程：

- `ros2-doc/4-服务与参数管理/4-服务与参数管理.pdf`

## 本次目标

在上一节 `differential_drive_robot` 的基础上，把简单话题控制升级成控制框架：

- 键盘节点发布遥控命令和控制模式。
- 控制器节点支持多种控制模式。
- 使用 ROS2 service 切换模式。
- 使用 ROS2 parameter 管理速度、安全距离、墙面跟随参数。
- 保持 Gazebo 仿真可运行、可验证。

## 已实现内容

### 1. 新增自定义服务

文件：

```text
ros2-src/differential_drive_robot/srv/SetControlMode.srv
```

内容：

```srv
string mode
---
bool success
string message
```

服务名：

```text
/set_control_mode
```

支持模式：

```text
manual
auto_forward
auto_circle
obstacle_avoidance
wall_following
```

### 2. 参数配置文件

文件：

```text
ros2-src/differential_drive_robot/config/controller_params.yaml
```

核心参数：

```yaml
max_linear_speed: 0.26
max_angular_speed: 1.82
max_linear_acceleration: 0.5
max_angular_acceleration: 1.0
command_timeout: 0.7
obstacle_stop_distance: 0.45
wall_target_distance: 0.55
wall_kp: 1.0
control_rate: 20.0
```

这些参数在 `gazebo_harmonic.launch.py` 中加载给 `robot_controller`。

### 3. KeyboardController 升级

`teleop_keyboard.cpp` 从简单速度发布器升级为：

- 发布 `/teleop_cmd`：`geometry_msgs/msg/TwistStamped`
- 发布 `/control_mode`：`std_msgs/msg/String`

按键：

```text
w/x: 前进/后退
a/d: 左转/右转
s/空格: 停止
1: manual
2: auto_forward
3: auto_circle
4: obstacle_avoidance
0: wall_following
q: 退出
```

### 4. RobotController 升级

`robot_controller.cpp` 新增：

- 订阅 `/teleop_cmd`
- 订阅 `/control_mode`
- 订阅 `/scan`
- 订阅 `/odom`
- 订阅 `/joint_states`
- 发布 `/cmd_vel`
- 提供 `/set_control_mode` 服务
- 支持参数动态更新
- 支持速度限制和速度平滑
- 支持安全检查和紧急停车

控制模式：

- `manual`：使用键盘遥控命令。
- `auto_forward`：自动直行。
- `auto_circle`：自动圆形运动。
- `obstacle_avoidance`：前方近距离障碍时转向，否则前进。
- `wall_following`：根据右侧距离做简单墙面跟随。

说明：当前自制机器人模型还没有真实雷达，但控制器已经兼容 `/scan`。没有 `/scan` 时自动模式仍可运行；后续加雷达后安全逻辑会自动生效。

## 验证命令

构建：

```bash
cd ~/ros2_ws
colcon build --symlink-install --packages-select differential_drive_robot
source install/setup.bash
```

查看服务接口：

```bash
ros2 interface show differential_drive_robot/srv/SetControlMode
```

结果：

```text
string mode
---
bool success
string message
```

启动仿真：

```bash
ros2 launch differential_drive_robot gazebo_harmonic.launch.py rviz:=false
```

查看服务：

```bash
ros2 service list | grep set_control_mode
```

结果：

```text
/set_control_mode
```

查看参数：

```bash
ros2 param list /robot_controller
```

能看到：

```text
max_linear_speed
max_angular_speed
max_linear_acceleration
max_angular_acceleration
command_timeout
obstacle_stop_distance
wall_target_distance
wall_kp
control_rate
```

调用服务切换圆形运动：

```bash
ros2 service call /set_control_mode differential_drive_robot/srv/SetControlMode \
  '{mode: auto_circle}'
```

结果：

```text
success=True
message='mode changed to auto_circle'
```

检查速度输出：

```bash
ros2 topic echo /cmd_vel --once
```

结果：

```text
linear.x: 0.14
angular.z: 0.45
```

检查里程计：

```bash
ros2 topic echo /odom --once
```

结果显示机器人已经在运动，位置和姿态发生变化。

## 测试结果

```bash
colcon test --packages-select differential_drive_robot
colcon test-result --all
```

结果：

```text
differential_drive_robot: 6 tests, 0 errors, 0 failures, 0 skipped
总体：22 tests, 0 errors, 0 failures, 3 skipped
```

## 本次踩坑

### 1. 自定义 service 需要 `rosidl_default_generators`

`CMakeLists.txt` 中新增：

```cmake
find_package(rosidl_default_generators REQUIRED)
rosidl_generate_interfaces(${PROJECT_NAME}
  "srv/SetControlMode.srv"
)
rosidl_get_typesupport_target(cpp_typesupport_target ${PROJECT_NAME} "rosidl_typesupport_cpp")
target_link_libraries(robot_controller "${cpp_typesupport_target}")
```

`package.xml` 中新增：

```xml
<buildtool_depend>rosidl_default_generators</buildtool_depend>
<exec_depend>rosidl_default_runtime</exec_depend>
<member_of_group>rosidl_interface_packages</member_of_group>
```

### 2. `member_of_group` 在 `package.xml` 里的位置有要求

一开始放到 `<export>` 后面，`xmllint` 报错：

```text
Element 'member_of_group': This element is not expected.
```

修复：放到 `<export>` 前面。

### 3. uncrustify 对换行和缩进很严格

测试会给出精确 diff，按它提示改即可。

### 4. 当前模型还没有雷达

控制器已经订阅 `/scan`，但自制 URDF 还没有加激光雷达插件。后续可以继续扩展传感器模型，让 `obstacle_avoidance` 和 `wall_following` 更真实。

## 当前结论

第 4 节的服务与参数管理已经跑通：

- 自定义服务可用。
- 参数可查询。
- 服务切换模式可用。
- 自动圆形运动可用。
- 构建和测试通过。
